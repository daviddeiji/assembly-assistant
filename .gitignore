.claude/settings.local.json

# Real business data — never commit. These are the filenames the app
# generates for exports and backups, in case one is saved into this folder.
assembly-backup-*.json
assembly-entries-*.csv
assembly-accounts-*.xlsx
invoice-*.xlsx

# Invoice spec contains real company + bank details — keep it out of the public repo.
INVOICE_SPEC.md
